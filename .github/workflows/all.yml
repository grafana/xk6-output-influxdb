name: CI
on:
  # Enable manually triggering this workflow via the API or web UI
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - v*
  pull_request:

jobs:
  checks:
    uses: grafana/k6-ci/.github/workflows/all.yml@c9fd2482f918a4ece532611480ad59abeef86f1b # main